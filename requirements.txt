# ============================================================
# KayaChatBot Python Dependencies — version-pinned for
# reproducible Docker builds and local development.
#
# Pinned against: torch==2.6.0, CUDA 12.4, unsloth==2026.4.2
#
# To upgrade: bump the version here AND in Dockerfile, then
# rebuild the image with --no-cache to test compatibility.
# ============================================================

# --- Unsloth fine-tuning stack ---
# unsloth 2026.4.2: Gemma 4 support; xformers 0.0.29.post3 requires torch==2.6.0
# torchao 0.17.0: required by unsloth-zoo 2026.4.2 (must NOT be uninstalled)
unsloth==2026.4.2
unsloth-zoo==2026.4.2
xformers==0.0.29.post3  # requires torch==2.6.0
torchao==0.17.0          # required by unsloth-zoo>=2026.4.2 (do NOT uninstall)

# --- Transformers ecosystem ---
# 4.57.6: >=4.56.2 required by trl 0.29.1; not in unsloth-zoo blocklist
transformers==4.57.6  # trl==0.29.1 requires >=4.56.2
trl==0.29.1
peft==0.15.2
accelerate==1.5.2
bitsandbytes==0.45.5
datasets==3.6.0

# --- RAG / vector database ---
chromadb==1.0.0
sentence-transformers==3.4.1

# --- Data science ---
numpy==1.26.4
pandas==2.2.3
scipy==1.14.1
scikit-learn==1.6.1
PyYAML==6.0.2

# --- Tokenization ---
sentencepiece==0.2.1
protobuf==5.29.6
tiktoken==0.9.0

# --- LLM providers ---
openai==1.109.1
xai-sdk==1.1.0

# --- Utilities ---
tqdm==4.67.1
python-dotenv==1.0.1
colorama==0.4.6
ipython==8.30.0
