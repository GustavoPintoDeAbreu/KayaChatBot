# ============================================================
# KayaChatBot Python Dependencies — version-pinned for
# reproducible Docker builds and local development.
#
# Pinned against: torch==2.9.1, CUDA 12.8, unsloth==2026.4.5
#
# To upgrade: bump the version here AND in Dockerfile, then
# rebuild the image with --no-cache to test compatibility.
# ============================================================

# --- Unsloth fine-tuning stack ---
# unsloth 2026.4.5: Gemma 4 E4B fixes (use_cache, grad accum); trl capped at <=0.24.0
# torchao 0.17.0: requires torch>=2.7 (register_constant); compatible with torch 2.9.x
unsloth==2026.4.5
unsloth-zoo==2026.4.5
xformers==0.0.33.post2  # requires torch==2.9.1
torchao==0.17.0          # required by unsloth-zoo>=2026.4.2; needs torch>=2.7

# --- Transformers ecosystem ---
# transformers 5.5.0: max allowed by unsloth 2026.4.5 (<=5.5.0)
# trl 0.24.0: max allowed by unsloth 2026.4.5 (<=0.24.0); has SFTConfig
transformers==5.5.0
trl==0.24.0
peft==0.19.0
accelerate==1.5.2
bitsandbytes==0.45.5
datasets==3.6.0

# --- RAG / vector database ---
chromadb==1.0.0
sentence-transformers==3.4.1

# --- Data science ---
numpy==1.26.4
pandas==2.2.3
scipy==1.14.1
scikit-learn==1.6.1
PyYAML==6.0.2

# --- Tokenization ---
sentencepiece==0.2.1
protobuf==5.29.6
tiktoken==0.9.0

# --- LLM providers ---
openai==1.109.1
xai-sdk==1.1.0

# --- Utilities ---
tqdm==4.67.1
python-dotenv==1.0.1
colorama==0.4.6
ipython==8.30.0
