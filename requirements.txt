# ============================================================
# KayaChatBot Python Dependencies — version-pinned for
# reproducible Docker builds and local development.
#
# Pinned against: torch==2.6.0, CUDA 12.4, unsloth==2026.4.5
# (matches the validated kaya_chatbot_env venv — the app runs in Docker,
#  fine-tuning runs in the venv.)
#
# To upgrade: bump the version here AND in Dockerfile, then
# rebuild the image with --no-cache to test compatibility.
# ============================================================

# --- Unsloth fine-tuning stack ---
# unsloth 2026.4.5: Gemma 4 E4B fixes (use_cache, grad accum); trl capped at <=0.24.0
# Versions match the validated venv (torch 2.6.0 / cu124).
unsloth==2026.4.5
unsloth-zoo==2026.4.7    # min required by unsloth 2026.4.5
# xformers prebuilt for torch 2.6.0 / cu124 (matches the venv); from the PyTorch index.
xformers @ https://download.pytorch.org/whl/cu124/xformers-0.0.29.post3-cp312-cp312-manylinux_2_28_x86_64.whl
torchao==0.16.0          # version validated in the venv with torch 2.6.0

# --- Transformers ecosystem ---
# transformers 5.5.0: max allowed by unsloth 2026.4.5 (<=5.5.0)
# trl 0.24.0: max allowed by unsloth 2026.4.5 (<=0.24.0); has SFTConfig
transformers==5.5.0
trl==0.24.0
peft==0.19.0
accelerate==1.13.0
bitsandbytes==0.49.2
datasets==3.6.0

# --- RAG / vector database ---
chromadb==1.5.5
sentence-transformers==5.3.0

# --- Data science ---
numpy==1.26.4
pandas==2.2.3
scipy==1.14.1
scikit-learn==1.6.1
PyYAML==6.0.2

# --- Tokenization ---
sentencepiece==0.2.1
protobuf==5.29.6
tiktoken==0.12.0

# --- LLM providers ---
openai==1.109.1
xai-sdk==1.9.1  # >=1.7 needed for xai_sdk.tools.web_search (web-search Agent Tool)

# --- Web UI ---
gradio==6.15.1

# --- WhatsApp bridge (src/chat/whatsapp_server.py) ---
# fastapi/uvicorn/httpx ship transitively with gradio 6.15; pinned here so the
# WhatsApp webhook server + WAHA client are explicit, reproducible deps.
fastapi==0.136.3
uvicorn==0.42.0
httpx==0.28.1

# --- Utilities ---
psutil==7.2.2           # train.py memory reporting
tqdm==4.67.1
python-dotenv==1.0.1
colorama==0.4.6
ipython==8.30.0

# --- Image generation / editing ---
# diffusers used to arrive unpinned, transitively from unsloth. It is not a
# transitive detail: PREFERRED_KONTEXT_RESOLUTIONS and the pipeline's _auto_resize
# behaviour decide what resolution a face reaches the transformer at, and a
# resolver picking a different version would change that silently.
diffusers==0.37.1
# Face detection for edit framing and for choosing between candidate takes
# (src/chat/face_utils.py). CPU only — the card is holding the pipeline.
insightface==1.0.1
onnxruntime==1.24.4
# opencv-python, NOT -headless: insightface declares `opencv-python` by name, so
# installing only the headless build leaves it unsatisfied and pip pulls the
# regular one anyway. 4.11 is the last line that still accepts numpy 1.26 — from
# 4.12 and all of 5.x the wheels require numpy>=2, which this stack is pinned
# below (numpy==1.26.4, load-bearing for torch 2.6 / scipy / chromadb).
opencv-python==4.11.0.86

# --- Testing (used by the kaya-test container / CI) ---
pytest==9.0.2
av==18.0.0
ctranslate2==4.8.1
faster-whisper==1.2.1
piper-tts==1.6.0
